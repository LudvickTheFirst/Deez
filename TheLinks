import java.awt.*;

public class TheLinks extends Character {


    public TheLinks(Color color, int x, int y, int diameter) {
        super(color, x, y, diameter, diameter);
    }

    @Override
    public void paint(Graphics g){
        g.setColor(color);
        g.fillOval(x, y, width, height);
    }
}
